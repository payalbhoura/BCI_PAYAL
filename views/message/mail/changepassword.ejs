<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
            text-align: center;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 30px;
        }

        h1 {
            color: #007bff;
            margin-bottom: 20px;
        }

        p {
            font-size: 18px;
            margin-bottom: 20px;
        }

        a {
            color: #007bff;
            text-decoration: none;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Welcome to Our Site!</h1>
        <p>Hello User,</p>
        <p>We hope you're doing well. Thank you for visiting our site. You can now change your password by clicking on the following link:</p>
        <p><a id="resetLink" href="<%= resetUrl %>">Click Here</a></p>
        <p>Thank you for your visit!</p>
    </div>

    <script>
        // JavaScript to dynamically set the reset URL
        // var resetUrl = `http://localhost:8000/resetpassword/${resetToken}`; // Replace {{resetToken}} with the actual token value
        // document.getElementById("resetLink").href = resetUrl;
        // document.getElementById("resetLink").value = resetUrl;
    </script>
</body>

</html>
